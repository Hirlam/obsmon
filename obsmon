#!/bin/bash

export OBSMON_SRC_DIR="$(dirname "$(readlink -f "$0")")"
export OBSMON_R_SCRIPT="${OBSMON_SRC_DIR}/obsmon.R"
# Call R-script (or alternatively CMD BATCH)
Rscript ${OBSMON_R_SCRIPT} "$@" || R CMD BATCH ${OBSMON_R_SCRIPT} "$@" || exit 1
